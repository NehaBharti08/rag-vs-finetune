# Environment matrix

Generated by `scripts/00_smoke.sh` from `reports/smoke_results.json`.
**Do not hand-edit** — rerun the smoke job instead.

Measured `2026-08-07T11:38:26+0000` on `AESed`.

## Verdict: PASS

The pinned matrix loads Qwen2.5-7B in 4-bit NF4, attaches LoRA to all seven
projections, and takes real optimizer steps on this GPU.

## Why this file exists

The dependency conflict below is real and was present in this machine's base
environment before the project started:

| Package | Constraint |
|---|---|
| `bitsandbytes 0.50.0` | requires `transformers<5` |
| `trl 1.9.2` | requires `transformers>=4.56.2` |
| **Satisfiable window** | **`transformers>=4.56.2,<5`** |
| Base env on this machine | `transformers 5.14.1` on Python 3.13 — **outside the window** |

That is why this project uses its own `uv` venv pinned to Python 3.11.

## Verified versions

| Component | Version |
|---|---|
| Python | 3.11.15 |
| torch | 2.13.0+cu130 (CUDA build 13.0) |
| transformers | 4.57.6 |
| peft | 0.20.0 |
| trl | 1.9.2 |
| bitsandbytes | 0.50.0 |
| accelerate | 1.14.0 |

## Hardware

| Property | Value |
|---|---|
| GPU | NVIDIA RTX A5000 |
| Compute capability | 8.6 |
| Total VRAM | 22.05 GiB |
| bf16 supported | True |

No SLURM on this host — verified absent: `sbatch`, `squeue`, `sinfo`, module
system, and any cluster entry in `~/.ssh/config`. Jobs run under tmux via
`scripts/run.sh`.

## Configuration measured

| Setting | Value |
|---|---|
| Model | `Qwen/Qwen2.5-7B-Instruct` |
| Quantization | nf4 double=True |
| LoRA rank / alpha | 16 / 32 |
| Target modules | `q_proj`, `k_proj`, `v_proj`, `o_proj`, `gate_proj`, `up_proj`, `down_proj` |
| Sequence length | 512 |
| Batch (per-device x accum = effective) | 2 x 8 = 16 |
| Gradient checkpointing | True |
| Optimizer | `paged_adamw_8bit` |
| Packing | True |

Trainable parameters: **40,370,176** of 4,393,342,464 (**0.9189%**).

## Measurements

| Metric | Value |
|---|---|
| Model load time | 13.2 s |
| Weights only (4-bit, post-load) | 5.18 GiB |
| **Peak allocated** | **11.13 GiB** |
| **Peak reserved** | **11.88 GiB** |
| Headroom on a 22.05 GiB card | 10.17 GiB (46%) |
| Mean step time (warmup discarded) | 51.884 s |
| Steps timed | 17 |
| **Throughput** | **631.6 tokens/s** |
| Final loss | 0.0076 |

Headroom matters more than speed here: this GPU is shared, so a run that peaks
near capacity is a run that dies when someone else allocates memory.

## Recalibrated compute budget

Projected from measured throughput, assuming 2,839
training examples averaging **113 tokens each**.

⚠️ The average example length — not `seq_len` — drives this. With packing on,
short examples are concatenated to fill each 512-token block, so
2,839 examples of ~113
tokens occupy roughly 626 sequences, not
2,839 of them. Using `examples x seq_len` instead
overstates the budget by the packing ratio (~4.5x here).

The token count itself is still an **estimate** until Phase 1 measures the real
length distribution of (passage + question + formatted answer). Throughput is
measured; the example length is not, yet.

| Item | GPU-hours |
|---|---|
| One epoch | 0.14 |
| One 3-epoch run | 0.42 |
| Sweep: 4 configs x 1 epoch | 0.56 |
| Final: 3 seeds x 3 epochs | 1.27 |
| **Training total** | **1.83** |

Evaluation, latency measurement and the smoke job add roughly 4.5 GPU-hours on
top, and are dominated by generation rather than training.
